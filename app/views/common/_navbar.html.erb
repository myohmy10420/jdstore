<nav class="navbar navbar-main" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header navbar-header--center">
      <a class="navbar-brand text--white" href="/" font-weight: bold; style="font-size: 26px;" ">JDStore</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <ul class="nav navbar-nav">
          <li class="active">
            <%= link_to("商品列表", products_path, class: "text--white") %>
          </li>
          <li>
            <%= link_to carts_path, class: "text--white" do  %>
              购物车 <i class="fa fa-shopping-cart text--white"> </i> ( <%= current_cart.products.count %> )
            <% end %>
          </li>
        </ul>
        <% if !current_user %>
          <li><%= link_to("注册", new_user_registration_path, class: "text--white") %> </li>
          <li><%= link_to(content_tag(:i, '登入', class: 'fa fa-sign-in text--white'), new_user_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle text--white" data-toggle="dropdown">
              Hi!, <%= current_user.email %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
                <li>
                  <%= link_to("Admin 选单", admin_products_path, class: "text--white" ) %>
                </li>
              <% end %>
              <li>
                <%= link_to("个人订单列表", account_orders_path ) %>
              </li>
              <li> <%= link_to(content_tag(:i, '登出', class: 'fa fa-sign-out'), destroy_user_session_path, method: :delete) %> </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
